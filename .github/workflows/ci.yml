name: CI

on:
  pull_request:
  push:
    branches:
      - main
      - "feat/**"

permissions:
  contents: read

env:
  PYTHONPATH: src

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Set up uv
        uses: astral-sh/setup-uv@v8
        with:
          enable-cache: true

      - name: Lint
        run: uv run --with-requirements requirements-dev.txt ruff check src tests

      - name: Test
        run: >
          uv run --with-requirements requirements-dev.txt
          pytest --cov=src --cov-report=term-missing

      - name: Benchmark
        run: |
          if [ -f src/benchmark.py ] && [ -f tests/fixtures/benchmark_cases.json ]; then
            uv run --with-requirements requirements-dev.txt python -m benchmark tests/fixtures/benchmark_cases.json
          else
            echo "benchmark is not implemented yet"
          fi
